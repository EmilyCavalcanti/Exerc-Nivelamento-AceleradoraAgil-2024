Faça um algoritmo que leia uma quantidade não determinada de números positivos:
Entrada de dados que armazena as informações fornecedias;
Transformar os dados de string em number.


Calcule a quantidade de números pares e ímpares:
Criar variavel par e variavel impar fora da função, que servirão como contador;
Montar a estrutura if, e dentro colocar as condições que definam quais dados vao para a variavel par e quais vao para a variavel impar;
Variavel par++ ou impar++.

Calcule média de valores pares:
Criar o array para salvar os valores pares, dentro do if;
Através do método push salvarão os valores dentro do array;
Criar uma variavel de soma fora da função, nela será armazenado o resultado da soma dos valores;
Criar um for para percorrer os números do array e somar os valores;
Calculo divindo o valor armazenado em soma pelo valor armazenado em numPar = mediaPar.

Calcule a média geral dos números lidos.
Criar a variavel de array, fora da função, para armazenar todos os numeros da entrada;
Através do método push, após converter os dados para numero;
Criar uma variavel de soma fora da função, nela será armazenado o resultado da somaTotal dos valores;
Criar um for para percorrer os números do array e somar os valores;
Calculo divindo o valor armazenado em somaTotal por numPar + numImpar = mediaTotal.

O número que encerrará a leitura será 0
Uso do if ternário

Console.log
numPar , numImpar, mediaPar, mediaTotal


----------------------------------------------------
//  

Faça um programa no qual o usuário informe o número de médias a serem inseridas: 
1-Criar uma coleção de objetos que irá receber um array para cada aluno, nome do aluno será string e media será number.
ex: let medias_alunos:{
    [aluno: "x", média: 8]
}
2- Criar a entrada do usuário e converter o valor para number.
3- Uma entrada para informar a quantidade de alunos 
4- Outra entrada para informar o nome do aluno e sua média.Atribuir os valores para a coleção de objetos que será criada fora da função.

e após leia cada uma das médias juntamente com o nome do aluno:

1-acessar a coleção do objeto com um loop e imprimir na tela as propriedades.


Ao final do programa deverá informar a maior média juntamente com o nome do aluno que a obteve:
1- Fazer um for para percorrer a propriedade media do objeto e dentro do for criar um if para comparar os valores das médias 
2-imprimir o maior valor com o nome.

***************************

//Faça um programa que leia o nome e a média de alunos, o programa deverá parar quando o nome de aluno informado for igual a FIM. Quando terminar, o programa deverá informar a média de todos os alunos e o numero de alunos que tiveram suas médias informadas 

criar a classe Alunos, criando e tipando os atributos e os metódos;

entrada de nome e media dos alunos, 
colocar a cond